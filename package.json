{
  "name": "chord-app",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "axios": "^0.16.2",
    "react-scripts": "0.7.0"
  },
  "dependencies": {
    "react": "^15.3.2",
    "react-dom": "^15.3.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && cp -r ./build/* ../chord-app-server-side/public",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
